name: apitest
description: "A new Flutter project."
publish_to: "none"
version: "1.0.0+2"

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.5.0
  get_it: ^9.2.1
  dio: ^5.11.0
  equatable: ^2.1.0
  meta: ^1.18.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
